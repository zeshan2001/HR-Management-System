<%- include('../partials/_header') %>

<div class="container">
  <a href="/employees/new">add an employee</a>
  <br />

  <h1 class="mb-4">list of employees</h1>
  <div class="row">
    <% employees.forEach(employee => { %>

    <li>
      <a href="/employees/<%= employee._id %>"> <%= employee.name %> </a>
    </li>

    <!-- 
    /.col-lg-4
    <div class="col-lg-4">
      <svg
        aria-label="Placeholder"
        class="bd-placeholder-img rounded-circle"
        height="140"
        preserveAspectRatio="xMidYMid slice"
        role="img"
        width="140"
        xmlns="http://www.w3.org/2000/svg"
      >
        <title>Placeholder</title>
        <rect
          width="100%"
          height="100%"
          fill="var(--bs-secondary-color)"
        ></rect>
      </svg>
      <h2 class="fw-normal"><%= employee.name %></h2>
      <p>
        <a class="btn btn-secondary" href="/employees/<%= employee._id %>"
          >details »</a
        >
      </p>
    </div>
    /.col-lg-4 -->
    <% }) %>
  </div>
</div>
<%- include('../partials/_footer') %>
