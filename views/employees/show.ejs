
<%- include('../partials/_header') %>

<div class="container my-5">
  <table class="table table-striped-columns my-5">
    <tbody>
      <tr>
        <td><b>Name</b></td>
        <td><%= employee.name%></td>
      </tr>
      <tr>
        <td><b>Email</b></td>
        <td><%= employee.email%></td>
      </tr>
      <tr>
        <td><b>Phone</b></td>
        <td><%= employee.phone%></td>
      </tr>
      <tr>
        <td><b>Salary</b></td>
        <td><%= employee.salary%></td>
      </tr>
      <tr>
        <td><b>Department</b></td>
        <td><%= employee.department%></td>
      </tr>
    </tbody>
  </table>
  
      <div class="d-flex justify-content-end align-items-center">
        <form class="mx-5" action="/employees/<%= employee._id %>/edit" method="get">
          <button class="btn btn-primary w-100 py-2" >Edit</button>
        </form>

        <form
          class=""
          action="/employees/<%= employee._id %>?_method=DELETE"
          method="post">
          <button class="btn btn-primary w-100 py-2" type="submit">Delete</button>
        </form>
      </div>
</div>

<%- include('../partials/_footer') %>